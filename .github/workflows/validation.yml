name: Validation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          pip install -r requirements/dev.txt
      - name: Validate Storage Architecture
        run: python scripts/validate_storage.py
      - name: Lint (Compile Check)
        run: python -m compileall .
      - name: Runtime Boot Validation
        run: |
          PYTHONPATH=. python -c "from core.runtime.runtime import Runtime; r = Runtime(); r.bootstrap(); r.shutdown()"
      - name: Run Core Tests
        run: |
          PYTHONPATH=. pytest tests/test_runtime.py
      - name: Run Governance Simulations
        run: |
          PYTHONPATH=. pytest tests/test_governance_sim.py
      - name: Run Knowledge Fabric Tests
        run: |
          PYTHONPATH=. pytest tests/test_knowledge_fabric.py
