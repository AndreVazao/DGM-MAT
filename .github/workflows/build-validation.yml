---
name: Build Validation

"on":
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      - name: Build with PyInstaller
        run: |
          pyi-makespec --onefile --name dgm_mat core/runtime/runtime.py
          pyinstaller dgm_mat.spec
      - name: Run Startup Smoke Test
        env:
          PYTHONPATH: .
        run: python scripts/executable_runtime_test.py
